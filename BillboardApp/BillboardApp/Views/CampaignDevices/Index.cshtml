@model IEnumerable<BillboardApp.Models.CampaignDevice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Campaign.CampaignName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Device.IMEI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Campaign.CampaignName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Device.IMEI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CampaignDeviceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CampaignDeviceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CampaignDeviceID })
        </td>
    </tr>
}

</table>
